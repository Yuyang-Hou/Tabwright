## 1. Release workflow

- [x] 1.1 Add a main-only manual CLI release that typechecks, tests, builds, publishes to npm, and creates the latest GitHub Release
- [x] 1.2 Add a main-only extension release that verifies the matching CLI, builds a clean production ZIP, and creates a non-latest GitHub Release

## 2. Verification

- [x] 2.1 Validate the workflow YAML and OpenSpec change
- [x] 2.2 Review the final diff and merge it with the prepared 3.4.0 release
