---
'playwriter': minor
---

Add operation-level capability policies so one saved capability can expose autonomous read operations and separately confirmed write operations. Replace the built-in Conan config search/query capabilities with one unified domestic production/test capability for search, read, schema and history inspection, validated content changes, drafts, publishing, config creation, definition copying, and verified rollback.
