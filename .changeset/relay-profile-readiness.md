---
'playwriter': patch
---

Wait for the current or a newer compatible relay package version after restart, let reconnecting Chrome profiles settle, and automatically select only the unambiguous profile with enabled tabs.
