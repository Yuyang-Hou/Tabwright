---
'tabwright': patch
---

Treat declared browser authentication origins as valid runtime network access, quarantine contract failures per operation, and let repaired operations clear quarantine through an explicit `--force` validation run without reinstalling the Skill.
