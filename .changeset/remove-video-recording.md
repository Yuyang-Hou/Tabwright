---
"playwriter": major
---

Remove the legacy video recording product path, including extension capture permissions, offscreen capture files, video replay UI, relay storage, and `recording.*` execute-context APIs. Replay recording now uses rrweb DOM capture only.
