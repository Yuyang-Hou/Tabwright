---
"playwriter": minor
---

Add an rrweb replay compiler that turns supported demonstrations into runnable browser workflow capabilities with `needs_ai` handoff on validation or page drift. Includes `playwriter replay make` for the common replay-to-capability flow and a `playwriter replay eval` self-test platform that creates local example pages, indexes recordings, compiles capabilities, runs them in a real browser, and verifies the result.
