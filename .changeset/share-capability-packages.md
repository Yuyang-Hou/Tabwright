---
'playwriter': minor
---

Add safe capability packaging and installation from directories, local `.tgz` archives, and HTTPS `.tgz` URLs. Shared packages exclude local secrets, run logs, and artifacts, and always install as drafts.
